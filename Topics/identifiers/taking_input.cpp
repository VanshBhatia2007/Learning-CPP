#include <iostream>
using namespace std;

int main() {
    //to take input from the user
    //we'll create a bucket/variable to store the input inside it
    int a = 0;
    cin >> a; //cin is used to take input from the user
    cout << "you entered: "<<a; //cout is used to print the output to the console
    return 0;
}