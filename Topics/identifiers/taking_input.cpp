#include <iostream>
#include <string>
using namespace std;

int main() {
    //to take input from the user
    //we'll create a bucket/variable to store the input inside it
    string name;
    cin >> name; //cin is used to take input from the user
    cout << name; //cout is used to print the output to the console
    int a;
    cin >> a; //cin is used to take input from the user
    cout << a; //cout is used to print the output to the console
    return 0;
}